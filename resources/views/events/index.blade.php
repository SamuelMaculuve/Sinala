@extends('layouts.app',['title'=>'Eventos — Sinala']) @section('content')<div class="flex justify-between"><div><p class="eyebrow">GESTÃO</p><h1 class="mt-2 text-4xl font-bold">Eventos</h1></div><a class="btn-primary" href="{{ route('events.create') }}">+ Novo evento</a></div><div class="mt-8 grid gap-5 md:grid-cols-2 xl:grid-cols-3">@forelse($events as $event)<a href="{{ route('events.show',$event) }}" class="rounded-3xl border bg-white p-6 transition hover:-translate-y-1 hover:shadow-xl"><div class="flex justify-between"><span class="badge-success">{{ ucfirst($event->status) }}</span><span class="text-stone-400">{{ $event->starts_on->format('d/m/Y') }}</span></div><h2 class="mt-6 text-2xl font-bold">{{ $event->name }}</h2><p class="mt-2 text-stone-500">{{ $event->location }}</p><div class="mt-6 border-t pt-4 text-sm"><strong>{{ $event->participants_count }}</strong> participantes</div></a>@empty<p>Ainda não existem eventos.</p>@endforelse</div><div class="mt-8">{{ $events->links() }}</div>@endsection
